export const onMessageFunctions = async (msg, client) => {
  // TODO: read from config if right channel move to another channel
  // moveMessage(msg, client)
  // TODO: calculate exp on msg send
  // gainExp(msg, client)
  // TODO: remove admin message if muted by roulette
  //muteCinkito(msg, client)
};
